"""Nunes AI - the memory that makes an AI agent safe to trust with money.

Every onchain action is gated by a persistent, append-only Sibyl memory.
If the memory layer is removed, the agent becomes dangerous.
"""

__version__ = "0.1.0"
